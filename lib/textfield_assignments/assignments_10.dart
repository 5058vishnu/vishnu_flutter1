import 'package:flutter/material.dart';
class TextfieldAssignments10 extends StatelessWidget {
  const TextfieldAssignments10({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [
          TextField(
            ),
        ],
      ),
    );
  }
}
